[defaults]
timeout = 60
roles_path = ./roles
inventory = inventory.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = /home/jojo/.ssh/task-app

[ssh_connection]
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s

[privilege_escalation]
timeout = 60
